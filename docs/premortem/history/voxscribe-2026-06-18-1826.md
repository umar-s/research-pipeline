---
plan: voxscribe
горизонт: После первого реального прогона voxscribe на медиафайле пользователя
создан: 2026-06-18
обновлён: 2026-06-18
запусков: 1
version: '1.0'
session_recommendation: continue
---
# Премортем: voxscribe

## Контекст
- Reference class: loop-foundry / co-rar / research-pipeline (мои skill-плагины), premortem BA-91 (verify-by-fact на живой среде поймал то, что mock пропустил)

## Дыры

### H-001: medium+CUDA/fp16 default → OOM-краш на 4GB GPU на первом прогоне
- **Угол зрения:** Допущения, Граничные случаи
- **Найдена:** 2026-06-18 (запуск 1)
- **Важность:** высокая
- **Уверенность:** подкреплено контекстом
- **Статус:** ПРИНЯТО
- **Режим:** полный
- **Описание:**
  [verified] whisper default --device cuda --fp16 True; машина RTX A400 4GB, ~2GB free; medium ~5GB VRAM → torch.cuda.OutOfMemoryError ДО транскрипции. Спека предполагала «CPU медленно», реальность — hard OOM-краш. CPU-fallback whisper сам не делает.
- **Решение:** transcribe.sh авто-детектит свободный VRAM (nvidia-smi memory.free) vs потребность модели; не влезает или нет GPU → --device cpu --fp16 False; дефолт-модель small (а не medium); всё переопределяемо --device/--model
  - Зачем выбрали: Горизонт = первый реальный прогон; незакреплённый device = гарантированный краш, а не деградация скорости
  - Первые шаги (prevent):
    1. §4.5: nvidia-smi free vs таблица VRAM (tiny/base~1,small~2,medium~5,large~10)
    2. дефолт model=small
    3. на cpu форс --fp16 False
    4. smoke: дефолтный прогон не падает OOM
  - Исполнитель: агент
  - Сигнал успеха (detect): дефолтный прогон отдаёт текст, не torch.cuda.OutOfMemoryError
  - Стоп-условие: OOM/traceback на первом прогоне с дефолтами
  - Если уже случилось (limit damage): явный --device cpu вручную

### H-002: whisper exit 0 = ложный успех: пустой/галлюцинированный txt на не-речи
- **Угол зрения:** Исполнитель
- **Найдена:** 2026-06-18 (запуск 1)
- **Важность:** высокая
- **Уверенность:** подкреплено контекстом
- **Статус:** ПРИНЯТО
- **Режим:** полный
- **Описание:**
  [verified] на тишине/синусе whisper exit 0, stderr чист, пишет 0-байтный или галлюцинированный (25-байт) .txt. Скрипт рапортует успех по наличию файла, не по содержанию. set -euo pipefail не ловит. Паттерн BA-91 false-green.
- **Решение:** output-sanity guard после whisper: .txt пуст/whitespace ИЛИ .json без сегментов/высокий no_speech_prob → явное предупреждение + exit 4, не молчаливый успех
  - Зачем выбрали: Молчаливая пустая/ложная расшифровка хуже падения — подрывает доверие с 1-го прогона, травит downstream данными-призраками
  - Первые шаги (prevent):
    1. §4.8: проверка непустоты .txt + наличия сегментов в .json
    2. exit 4 на пустой транскрипции
    3. печать реального текста, не только путей
    4. smoke: тишина → guard срабатывает
  - Исполнитель: агент
  - Сигнал успеха (detect): на нормальном аудио .txt непустой и осмысленный; на тишине — явное предупреждение
  - Стоп-условие: exit 0 при пустом/мусорном .txt
  - Если уже случилось (limit damage): пере-прогон с верным языком/моделью

### H-003: дефолт --language Russian форсит русский на не-русском медиа → галлюцинация
- **Угол зрения:** Клиент
- **Найдена:** 2026-06-18 (запуск 1)
- **Важность:** высокая
- **Уверенность:** подкреплено контекстом
- **Статус:** ПРИНЯТО
- **Режим:** полный
- **Описание:**
  whisper при заданном --language НЕ детектит, а форсит: на англ/укр/каз аудио отдаёт уверенный русский текст (перевод-галлюцинация/транслитерация), без ошибки и предупреждения. Файл выглядит успешно, содержимое — мусор.
- **Решение:** дефолт --language auto (флаг не передаём, whisper детектит сам); Russian/иной — только явным --language
  - Зачем выбрали: Самый коварный отказ — правдоподобный неверный результат; первый файл пользователя может быть не русским
  - Первые шаги (prevent):
    1. §4.6: дефолт auto, опускаем --language
    2. --language как явный override
    3. smoke: англ. аудио не форсится в русский
  - Исполнитель: агент
  - Сигнал успеха (detect): язык определяется по содержимому, не форсится
  - Стоп-условие: уверенный русский на явно не-русском входе
  - Если уже случилось (limit damage): ручной --language ru

### H-004: рассинхрон marketplace name/source/версий → install молча падает или ломает соседа
- **Угол зрения:** Интеграции
- **Найдена:** 2026-06-18 (запуск 1)
- **Важность:** высокая
- **Уверенность:** подкреплено контекстом
- **Статус:** ПРИНЯТО
- **Режим:** полный
- **Описание:**
  Добавление 2-й записи в marketplace.json: если plugin.json name ≠ plugins[].name или source ≠ реальному каталогу — /plugin install молча не грузит skill (триггер не активируется), либо marketplace add падает на валидации и ломает уже работающий research-pipeline. JSON-lint синтаксиса этого не ловит.
- **Решение:** запись строго по образцу research-pipeline (name=voxscribe байт-в-байт, source=./plugins/voxscribe, author Sergei, owner umar-s, version 1.0.0); существующий плагин нетронут; верификация реальным /plugin marketplace add + install обоих + рестарт
  - Зачем выбрали: Критерий успеха «не ломает существующий research-pipeline» — нарушается тихо; нужен реальный install-тест
  - Первые шаги (prevent):
    1. §3: точный синк name/source/version
    2. §7: реальный /plugin marketplace add + install
    3. проверка что старый плагин жив после
    4. python3 -m json.tool обоих манифестов
  - Исполнитель: агент
  - Сигнал успеха (detect): оба плагина грузятся после install + рестарта
  - Стоп-условие: старый плагин пропал или skill не активируется
  - Если уже случилось (limit damage): откат записи в marketplace.json

### H-005: жёсткая привязка к openai-whisper без preflight → тихая поломка на иной машине
- **Угол зрения:** Будущий поддерживающий
- **Найдена:** 2026-06-18 (запуск 1)
- **Важность:** высокая
- **Уверенность:** подкреплено контекстом
- **Статус:** ПРИНЯТО
- **Режим:** полный
- **Описание:**
  transcribe.sh зовёт whisper как данность. На macOS/публичном MIT-юзере типична whisper.cpp (иной бинарь/флаги) или faster-whisper или нет GPU. Скрипт не проверяет бинарь, не даёт actionable-ошибку, флаги openai-whisper дрейфуют между версиями → через полгода/на другой машине молча ломается, чинить некому.
- **Решение:** preflight command -v whisper ffmpeg ffprobe с actionable-ошибкой; зафиксировать поддерживаемое окружение (Linux+openai-whisper) в options.md; macOS/whisper.cpp — known limitation v1, loud fail не silent
  - Зачем выбрали: «Ставится через /plugin marketplace» создаёт ожидание «работает у всех», а MIT-репо отдаёт скрипт людям без ~/.local/bin/whisper
  - Первые шаги (prevent):
    1. §4.2: preflight deps + exit 2 с инструкцией установки
    2. options.md: явное поддерживаемое окружение + known limitations
    3. версионная заметка про дрейф флагов whisper
  - Исполнитель: агент
  - Сигнал успеха (detect): отсутствие зависимости → понятная ошибка установки, не traceback
  - Стоп-условие: тихий traceback вместо «установи whisper»
  - Если уже случилось (limit damage): документированный fallback-движок

## Топ 1–3 — с чего начать
1. **H-001** — medium+CUDA/fp16 default → OOM-краш на 4GB GPU на первом прогоне. Первый шаг: §4.5: nvidia-smi free vs таблица VRAM (tiny/base~1,small~2,medium~5,large~10)
2. **H-002** — whisper exit 0 = ложный успех: пустой/галлюцинированный txt на не-речи. Первый шаг: §4.8: проверка непустоты .txt + наличия сегментов в .json
3. **H-004** — рассинхрон marketplace name/source/версий → install молча падает или ломает соседа. Первый шаг: §3: точный синк name/source/version

### Bias-проверка топа
- **Главный риск искажения:** Availability/WYSIATI — план собран там, где whisper в PATH и есть GPU; реальные получатели иные.
- **Коррекция:** H-005 preflight + зафиксированное окружение; H-001 device/VRAM-детект; H-004 реальная install-верификация.

